import 'package:flutter/material.dart';

class SecretaryGradeOverviewScreen extends StatelessWidget {
  const SecretaryGradeOverviewScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Notenübersicht'),
      ),
    );
  }
}
